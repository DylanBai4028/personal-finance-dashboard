# Public demo project (synthetic data, safe to share the shape of)
SUPABASE_DEMO_URL=
SUPABASE_DEMO_SERVICE_ROLE_KEY=

# Private personal project (real data, local-only, never deployed)
SUPABASE_PERSONAL_URL=
SUPABASE_PERSONAL_SERVICE_ROLE_KEY=

# Not needed by default — lib/llm_categorize.py shells out to the already-authenticated
# Claude Code CLI instead. Uncomment only if you swap to the Anthropic API path.
# ANTHROPIC_API_KEY=
